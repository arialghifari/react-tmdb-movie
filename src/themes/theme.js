import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    type: "light",
    primary: {
      main: "#0077cb",
    },
    secondary: {
      main: "#e63030",
    },
  },
});

export default theme;
